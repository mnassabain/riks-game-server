#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <string>
#include <sstream>

using namespace std;

/**
 * @brief spilt : split a string into a vector of multiple strings using a delimiter
 * @param s : string to be splitted
 * @param delim : delimiter
 * @return vector of splitted strings
 */
vector<string> split (string s, char delim);


#endif //UTILS_H
